local audio=script.Parent.Parent["layerlayer on the wall"]
local blueblue = script.Parent.middle.blueringyring
local redred=script.Parent.middle.redringyring
local thing=script.Parent.middle.thing
local thingagain=script.Parent.middle.thingagain
local blind1=script.Parent.middle.blind1
local blind2=script.Parent.middle.blind2
local blind3=script.Parent.middle.whiteringyring
local first2=1.65
local second2=2.75
local chaoschaos=20.9
local chaostime=117
local explosion=.25
local endomg = 53

audio:Play()

while true do
	task.wait(chaostime) 
	thing.Enabled=true 
	thingagain.Enabled=true
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	thing.Enabled=false
	thingagain.Enabled=false
	task.wait(first2)
	thing.Enabled=true
	thingagain.Enabled=true
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	thing.Enabled=false
	thingagain.Enabled=false
	wait(first2-explosion)
	thing.Enabled=true
	thingagain.Enabled=true
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	redred:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
	task.wait(explosion)
	blueblue:Emit(25)
    thing.Enabled=false
	thingagain.Enabled=false
	wait(second2*1.625)
    blind1.Enabled=true
	blind2.Enabled=true
	blind3.Enabled=true
	wait(chaoschaos/2)
	blind1.Enabled=false
	blind2.Enabled=false
	blind3.Enabled=false
	
	task.wait(56.5-endomg)
   
end